// Settings for client-side behavior in the app
export default function () {
  let settings = {
    maxWidth: 1400,
    maxHeight: 8000,
    fogOpacity: 1,
    fogRGB: "0,0,0",
    defaultLineWidth: 15,
    defaultBrushShape: 'round',
    mapImage: '/dm/map'
  };
  return settings;
}